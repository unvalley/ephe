"use client";

import { useRouter } from "next/navigation";
import { useEffect } from "react";
import Page from "../page";

export default function AppPage() {
    // This is a simple wrapper around the main app page
    return <Page />;
} 